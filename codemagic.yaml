workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      groups:
        - android-signing
    scripts:
      - name: Build Android APK
        script: cd app && flutter build apk --debug
    artifacts:
      - app/build/app/outputs/flutter-apk/*.apk
